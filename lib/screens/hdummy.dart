import 'package:flutter/material.dart';
class hdummy extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child:Text('Harshini',style: TextStyle(color: Colors.black),)

      ),
    );
  }
}